export default function AboutUsLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <div>{children} &copy; Next Js Is Greate</div>;
}

// route-group
// 괄호로 묶은 폴더들
// url 에 영향을 미치지 않는다.
